[package]
name = "sync-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
esp-idf-svc = { version = "0.49", features = ["binstart"] }
esp-idf-hal = "0.44"
esp-idf-sys = { version = "0.35", features = ["binstart"] }
anyhow = "1"
log = "0.4"

[build-dependencies]
embuild = "0.32"
